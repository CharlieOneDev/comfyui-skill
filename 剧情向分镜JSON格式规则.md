# 剧情向分镜 JSON 格式规则

## 1. 文档定位

这类内容不是普通的剧情大纲，也不是单纯给生成模型阅读的提示词，而是一种**可供程序按 JSON 层级、数组和字段进行拆分、重组的剧情分镜 DSL（Domain Specific Language）**。

因此写作时必须同时满足两件事：

1. **机器结构稳定**：字段名、层级、数据类型、数组关系必须稳定，不能为了文风随意改变结构。
2. **剧情信息完整**：每个分镜节点内部包含足够完整的视觉、动作、镜头、时间、对白和声音信息，使节点即使被单独抽取，也仍然能够理解并执行。

核心原则：**先保证 JSON 结构，再填充剧情；先保证节点独立可用，再追求语言润色。**

---

## 2. 顶层 JSON 固定结构

推荐保持以下固定顶层键，并按照此顺序组织：

```json
{
  "整体风格": "...",
  "角色档案": [],
  "道具档案": [],
  "场景档案": [],
  "分镜序列": []
}
```

### 2.1 `整体风格`

- 数据类型：字符串
- 作用：定义整个剧情项目的统一视觉与时代基调。
- 一般描述：
  - 美术风格
  - 时代/地域质感
  - 光照与时间氛围
  - 材质表现
  - 空气环境
- 特点：这是**全局约束**，不重复写在每个分镜里也能生效。

推荐写法：从“大方向”到“具体视觉质感”逐层收束，例如：

> 风格 → 时代地域 → 光色 → 材质 → 空气/环境颗粒

不要把具体分镜动作写进这里。

---

## 3. `角色档案`

- 数据类型：字符串数组
- 一个数组元素对应一个角色。
- 角色首次定义后，后续分镜可以直接使用角色姓名，不需要每镜重复完整外貌。

### 3.1 推荐角色档案信息顺序

```text
姓名 → 性别 → 年龄 → 脸部特征 → 发型 → 服装 → 配饰/随身物 → 鞋 → 特殊身体特征
```

### 3.2 角色档案的写法原则

角色档案承担的是**跨分镜的身份锚点**，应该尽量稳定、可复用。

建议写入：

- 姓名
- 性别
- 年龄或年龄段
- 身形
- 肤色
- 脸型/面部标志
- 发型/发色
- 固定服装
- 固定饰品
- 固定武器/腰带/鞋履
- 特殊识别特征

避免在角色档案里写：

- 某一镜才发生的动作
- 某一时刻的情绪
- 某一场景专属的姿势
- 会频繁变化的剧情状态

### 3.3 角色档案的重要性

角色档案不是人物小传，而是**视觉一致性字典**。

例如：

- “韩胖子”在所有分镜中都应指向同一个完整角色定义。
- “王护法”出现时，不应在不同分镜里重新发明服装、年龄或外貌。

---

## 4. `道具档案`

- 数据类型：字符串数组
- 一个数组元素对应一个可反复出现或具有剧情意义的道具。

### 4.1 道具档案的推荐信息顺序

```text
名称 → 材质 → 主色 → 结构 → 表面细节 → 尺寸/重量感 → 使用方式 → 固定位置 → 光照/运动表现
```

### 4.2 道具档案的功能

道具档案主要解决两个问题：

1. 同一道具跨分镜保持一致。
2. 在后续剧情中只使用名称，也能让模型/程序回溯到完整定义。

尤其适合：

- 旗帜
- 武器
- 书信
- 钱袋
- 首饰
- 关键机械
- 车辆
- 标志性物品

---

## 5. `场景档案`

- 数据类型：字符串数组
- 一个数组元素对应一个可复用场景/地点。

### 5.1 推荐场景信息顺序

```text
地点名称 → 时间/天气 → 主光源 → 地面 → 建筑/结构 → 中近景物件 → 远景 → 人群/环境动态 → 空气质感
```

### 5.2 场景档案的作用

场景档案是**世界空间字典**。

后续分镜只需要说明“青牛街”“春香酒楼后院厢房”等地点，就可以基于这里建立稳定的空间基础。

### 5.3 场景与镜头的区别

场景档案描述的是：

> “这个地方本来是什么样。”

分镜描述的是：

> “镜头此刻在这个地方看到了什么，以及人物正在做什么。”

因此不要把镜头运动、人物动作、具体对白塞进场景档案。

---

## 6. `分镜序列`

- 数据类型：对象数组
- 每一个数组元素是一个独立的剧情分镜节点。
- 这是整个 DSL 最重要的执行层。

标准结构：

```json
{
  "编号": 1,
  "标题": "青牛镇-镇口远望",
  "时间段": [
    "0-5s，...",
    "5-10s，...",
    "10-14s，..."
  ]
}
```

### 6.1 `编号`

- 数据类型：整数
- 作用：定义剧情顺序与节点 ID。
- 应连续递增。
- 不建议使用字符串编号。
- 不建议重复编号。

### 6.2 `标题`

- 数据类型：字符串
- 作用：快速识别该镜头节点的地点、动作或剧情功能。
- 推荐格式：

```text
地点-事件
地点-动作
地点-人物关系/剧情事件
```

例如：

```text
青牛镇-镇口远望
春香酒楼大堂-下车入内
春香酒楼门口夜-护法驾临
```

标题应短，不要把完整剧情塞进标题。

### 6.3 `时间段`

- 数据类型：字符串数组
- 每个数组元素代表一个**连续镜头片段/镜头动作节点**。
- 一个分镜可以包含 1 个或多个时间段。
- 时间段必须按时间递增排列。

推荐格式：

```text
"起始秒-结束秒，镜头方式。画面状态。人物动作。对白。声音。"
```

例如：

```text
"0-5s，大全景缓慢降镜。..."
"5-10s，微移切中景。..."
"10-14s，焦点转移..."
```

---

## 7. 时间段字符串的内部语法

虽然 `时间段` 是普通字符串，但内部实际上存在稳定的信息顺序。

建议按照以下逻辑组织：

```text
[时间范围]
→ [镜头类型/镜头运动]
→ [时间/光照]
→ [空间状态]
→ [人物位置]
→ [人物动作]
→ [人物表情/情绪]
→ [互动关系]
→ [对白/语言状态]
→ [声音/音效]
```

并非每段都必须包含全部项目，但应优先保证“镜头 + 画面动作 + 关键声音”存在。

---

## 8. 时间段不是单纯的“画面描述”

一个高质量时间段实际上是一个**可独立执行的微型镜头指令**。

推荐把内容分成三层理解：

### A. 镜头层

描述摄影机：

- 大全景
- 全景
- 中景
- 中近景
- 近景
- 特写
- 跟拍
- 固定机位
- 推镜
- 拉镜
- 横移
- 俯拍
- 仰拍
- 过肩
- 反打
- 焦点转移
- 从室内向外
- 从人物肩后向前

### B. 表演/动作层

描述人物：

- 谁在什么位置
- 正在做什么
- 动作方向
- 动作先后
- 与谁发生接触
- 手、脚、头、身体的具体动作
- 面部表情
- 情绪变化

尤其重要的是明确**动作主语和动作对象**，避免“他”“她”“对方”产生歧义。

### C. 声音层

通常放在句末：

```text
音效：...
```

包括：

- 环境声
- 动作声
- 物体声
- 马蹄声
- 门声
- 衣料摩擦声
- 人群声
- 风声
- 虫鸣
- 对白后的现场反应

---

## 9. 时间段内的信息组织原则

推荐采用“由外到内、由镜头到人物、由视觉到听觉”的顺序：

```text
镜头
→ 光线/时间
→ 环境
→ 人物位置
→ 人物动作
→ 表情与关系
→ 对白
→ 音效
```

不要在同一句话里毫无层次地随机堆信息。

---

## 10. 对白格式

对白直接嵌入对应时间段字符串中。

推荐格式：

```text
角色名说："对白内容"（口音：...，语速：...，情绪：...）
```

例如：

```text
韩胖子说："小立，你在这屋里好好休息下"（口音：东北腔，语速：缓，情绪：和蔼叮嘱）
```

### 10.1 对白必须明确说话者

不要只写：

```text
“你在这里休息。”
```

应该写：

```text
韩胖子说：“你在这里休息。”
```

这样在后续自动切分、重组或提取对白时不会产生歧义。

### 10.2 对白附加属性

推荐支持三个固定属性：

- `口音`
- `语速`
- `情绪`

可进一步扩展其他属性，但除非确有需求，不要频繁增加字段样式。

---

## 11. 音效格式

当时间段存在环境/动作声音时，统一使用：

```text
音效：声音A，声音B，声音C
```

建议放在该时间段末尾。

这样做的好处是程序或其他 AI 可以很容易识别“声音信息区”。

---

## 12. 镜头之间的连续性规则

虽然每个时间段可以独立执行，但整个分镜必须保持连续性。

### 12.1 空间连续

如果人物上一镜站在门口，下一镜应继续从合理的位置开始。

### 12.2 动作连续

例如：

```text
上一段：人物伸手抓住门帘
下一段：人物掀开门帘走出去
```

不要出现：

```text
上一段：人物站在床边
下一段：人物已经坐在街上
```

除非明确使用转场、跳切或时间跳跃。

### 12.3 物件连续

关键道具拿在谁手里、是否放下、是否佩戴，应保持一致。

### 12.4 光线连续

同一时间段内，光照不能无解释地突然从午后阳光切成深夜。

如果发生时间跳跃，应显式表达，例如：

```text
晨光、午后金色、傍晚橘红、墨蓝夜色在窗纸上急速流转，显示两日时间飞快过去。
```

---

## 13. 分镜节点的独立可执行原则

这是整个格式最重要的内容之一。

由于程序可能会：

- 单独读取某个编号
- 拆开 `时间段`
- 重新排序节点
- 拼接不同剧情
- 将单个节点交给其他 AI

因此每个分镜节点必须做到：

> **即使脱离前后镜头，也能根据角色档案、道具档案和场景档案理解当前画面。**

因此推荐在关键节点中适度重复：

- 人物当前所在位置
- 当前使用的关键道具
- 当前人物状态
- 与其他角色的相对关系
- 当前时间/光线

这种重复不是冗余，而是为了提高节点独立性。

---

## 14. “档案层 + 分镜层”的职责分离

整体结构可理解成四层：

```text
全局风格层
    ↓
角色/道具/场景档案层
    ↓
分镜节点层
    ↓
时间片段层
```

具体职责：

| 层级 | 作用 |
|---|---|
| 整体风格 | 全局美术、时代、光色、材质基调 |
| 角色档案 | 固定角色身份与视觉特征 |
| 道具档案 | 固定重要物件身份与视觉特征 |
| 场景档案 | 固定空间与环境特征 |
| 分镜序列 | 定义剧情顺序 |
| 编号 | 定义节点 ID |
| 标题 | 定义节点的人类可读名称 |
| 时间段 | 定义镜头级执行内容 |

---

## 15. 字段类型必须稳定

后续 AI 写这种 JSON 时，最忌讳的是“同一个概念换不同 JSON 类型”。

推荐固定：

```text
整体风格 = string
角色档案 = array<string>
道具档案 = array<string>
场景档案 = array<string>
分镜序列 = array<object>
编号 = integer
标题 = string
时间段 = array<string>
```

不要随意改成：

```json
"角色档案": {
  "韩立": "..."
}
```

或者：

```json
"时间段": "0-5s ..."
```

即使语义相同，也可能破坏后续程序解析逻辑。

---

## 16. 不建议增加额外 JSON 字段

除非程序端已经明确支持，否则不要随意增加：

```text
"镜头语言"
"对白"
"音效"
"人物动作"
"转场"
"BGM"
"情绪"
```

因为当前体系已经把这些内容编码在 `时间段` 字符串内部。

**不要为了看起来更结构化而自行“优化 JSON”。**

这套格式的特点恰恰是：

- 外层高度结构化
- 时间段内部保持自然语言叙事

如果程序已经按照当前结构解析，那么改变字段布局会直接破坏兼容性。

---

## 17. 为什么 `时间段` 使用字符串数组，而不是对象数组

当前设计类似：

```json
"时间段": [
  "0-5s，...",
  "5-10s，...",
  "10-14s，..."
]
```

而不是：

```json
"时间段": [
  {
    "开始": 0,
    "结束": 5,
    "镜头": "..."
  }
]
```

这意味着该格式的核心设计思想是：

> **以 JSON 负责粗粒度切分，以自然语言字符串承载细粒度导演指令。**

这样既方便程序按数组元素拆分，又能让每一个时间片段保持自然语言完整性。

不要在没有程序支持的情况下擅自把它改成对象结构。

---

## 18. 语言风格规则

这类文本不是文学小说，而是“高密度视觉执行文本”。

推荐：

- 具体
- 可视化
- 动作化
- 物理关系明确
- 镜头词明确
- 环境细节明确
- 声音具体
- 尽量避免抽象描述

### 18.1 少写抽象形容词

较弱：

> 他非常生气。

较强：

> 他眉头压低，嘴角绷紧，猛地把手里的杯子砸在桌面上。

即：

> **把“情绪结论”尽可能转译成“可以拍出来的行为”。**

### 18.2 少写无法执行的心理概念

例如：

> 他感到命运正在发生变化。

更适合写成：

> 他停下脚步，盯着门外突然出现的黑旗，原本松弛的手指一点点收紧。

---

## 19. 动作关系必须明确

为了避免 AI 在生成画面时错配人物关系，建议明确写：

```text
谁 → 对谁 → 做什么
```

而不是：

```text
两人握手。
```

更推荐：

```text
韩胖子伸手扶住韩立的右臂，将他从马车上搀下来。
```

复杂动作尤其应该明确：

- 左/右手
- 前/后
- 上/下
- 谁靠近谁
- 谁看向谁
- 谁站在哪里
- 谁拿着什么

---

## 20. 同一时间段应保持单一主要动作目标

例如一个时间段主要表现“下车”，就不要同时塞入大量无关剧情。

推荐：

```text
0-5s：下车
5-10s：进入酒楼
10-15s：与熟客互动
```

而不是把三段动作全部塞进一个 0-15s 时间段。

这样做可以提升：

- 程序切分质量
- 视频生成稳定性
- 镜头语义清晰度
- 后续重组能力

---

## 21. 每个时间段最好包含“可见变化”

一个时间片段最好至少存在一种明确变化：

- 摄影机变化
- 人物位置变化
- 人物动作变化
- 表情变化
- 视线变化
- 道具状态变化
- 光线变化
- 空间焦点变化
- 声音变化

避免长时间只有：

> 人物站在那里。

而没有任何镜头或动作推进。

---

## 22. 分镜节点的推荐内部节奏

一个完整分镜通常可以按照：

```text
建立环境
→ 展示人物
→ 发生动作
→ 关系变化
→ 对白/信息释放
→ 反应
→ 下一节点钩子
```

这不是硬性 JSON 规则，但非常适合剧情向视频。

例如：

```text
环境出现 → 马车接近 → 人物发现 → 马车停下 → 新人物出现
```

这种组织会比“单纯罗列镜头”更适合作为剧情 DSL。

---

## 23. 分镜之间应有“剧情钩子”

每个节点最好能给下一节点留下明确承接点。

例如：

```text
分镜 1：远处发现马车
↓
分镜 2：马车进入街道
↓
分镜 3：马车停在酒楼
↓
分镜 4：人物进入酒楼
```

这种写法的优势是：程序即使按照编号重组，也能形成自然的动作链。

---

## 24. 时间信息必须真实可切分

推荐写：

```text
0-5s
5-10s
10-15s
```

避免：

```text
前半段
中间
后面
```

因为明确时间范围可以直接作为程序切分依据。

### 24.1 时间范围应递增

推荐：

```text
0-4s
4-8s
8-13s
```

不推荐：

```text
0-5s
3-9s
8-15s
```

除非程序明确支持重叠区间。

---

## 25. 语义重复是允许的，但结构重复必须克制

允许在分镜中重复角色或道具的重要视觉信息，这是为了上下文稳定。

例如：

> 韩立穿着灰褐补丁短褐，穿旧草鞋。

但不需要每一段都重新写完整角色档案。

原则：

> **重复关键状态，不重复整份档案。**

---

## 26. 时间跳跃的写法

如果剧情发生小时、一天、数天的跳跃，应在镜头文字中显式说明。

例如：

```text
晨光暖白、午后金色、傍晚橘红、墨蓝夜色在窗纸上急速流转，显示两日时间飞快过去。
```

这种写法的作用不是单纯描写时间，而是告诉生成模型和程序：

> 当前画面发生了连续时间压缩。

---

## 27. 空镜与人物镜可以混用

允许一个分镜从空镜开始，然后进入人物动作：

```text
0-5s，环境空镜...
5-10s，中景人物...
```

这在剧情向作品中很有用，因为环境空镜可以：

- 建立场景
- 交代时间
- 交代危险来临
- 建立转场
- 形成悬念

---

## 28. 重要事件应通过“视觉证据”表现

不要只写：

> 有人身份很高贵。

应该展示：

- 特殊旗帜
- 独特车辆
- 周围人避让
- 服饰等级
- 武器
- 仪态
- 灯光
- 环境反应

这种写法让剧情信息能够在没有旁白的情况下成立。

---

## 29. “角色档案”与“角色行为”不可混淆

例如：

### 正确

角色档案：

```text
王护法，男，四十余岁，面庞瘦削……腰佩一柄黑鞘钢刀……
```

时间段：

```text
王护法从黑漆马车上一跃而下……
```

### 错误

角色档案直接写：

```text
王护法，男，四十余岁……从马车上一跃而下并冷哼……
```

因为这会让角色档案失去长期稳定定义的作用。

---

## 30. “场景档案”与“场景事件”不可混淆

场景档案描述静态空间基准；

分镜时间段描述该空间此刻发生的动态事件。

正确：

```text
场景档案：春香酒楼门口夜，红灯笼、青石板、黑漆马车……
```

分镜：

```text
王护法从黑漆马车上一跃而下……
```

---

## 31. 标题不是新的数据结构

不要给标题增加复杂对象：

```json
"标题": {
  "地点": "...",
  "事件": "..."
}
```

当前兼容格式要求标题保持纯字符串：

```json
"标题": "春香酒楼门口夜-护法驾临"
```

---

## 32. 推荐的生成流程

让其他 AI 生成这种 JSON 时，推荐严格按照以下顺序工作：

### 第一步：先确定世界规则

确定：

- 时代
- 地域
- 整体美术风格
- 光照风格
- 材质风格

### 第二步：建立角色档案

逐个固定角色视觉身份。

### 第三步：建立道具档案

只定义真正有复用价值或剧情价值的道具。

### 第四步：建立场景档案

按地点建立可复用环境锚点。

### 第五步：搭建分镜骨架

先只写：

```text
编号 + 标题 + 时间段
```

确定剧情顺序、地点和镜头节奏。

### 第六步：填充每个时间段

按照：

```text
镜头 → 环境 → 人物 → 动作 → 情绪 → 对白 → 音效
```

逐段补充。

### 第七步：做连续性检查

逐镜检查：

- 人物位置
- 道具持有状态
- 时间
- 光线
- 动作前后关系
- 对白说话者
- 声音

### 第八步：做 JSON 语法检查

确认：

- 所有键名用双引号
- 字符串用双引号
- 数组逗号正确
- 对象逗号正确
- JSON 内部不混入注释
- 不出现 Markdown 代码围栏

---

## 33. AI 输出时的硬性格式规则

这是最重要的兼容性部分。

### 必须保持

```text
顶层键名固定
数组/对象类型固定
字段名称固定
编号为整数
标题为字符串
时间段为字符串数组
分镜顺序按编号递增
```

### 不应发生

```text
把数组变成对象
把字符串变成对象
随意重命名字段
随意新增字段
随意删除字段
把一个时间段拆成另一套 JSON 结构
在 JSON 中加入解释文字
在 JSON 中加入 // 注释
```

---

## 34. 推荐的标准模板

下面模板用于要求其他 AI 按完全相同的格式生成剧情：

```json
{
  "整体风格": "整体视觉、美术、时代、地域、光照、材质、环境质感",
  "角色档案": [
    "角色1完整视觉定义",
    "角色2完整视觉定义"
  ],
  "道具档案": [
    "关键道具1完整视觉定义",
    "关键道具2完整视觉定义"
  ],
  "场景档案": [
    "场景1完整空间定义",
    "场景2完整空间定义"
  ],
  "分镜序列": [
    {
      "编号": 1,
      "标题": "地点-事件",
      "时间段": [
        "0-5s，镜头方式。环境与光线。人物位置和动作。关键关系。对白（如有）。音效：...",
        "5-10s，镜头方式。动作延续。人物表情变化。对白（如有）。音效：..."
      ]
    },
    {
      "编号": 2,
      "标题": "地点-事件",
      "时间段": [
        "0-5s，...",
        "5-10s，..."
      ]
    }
  ]
}
```

---

## 35. 最终检查清单

在输出前，对 AI 使用以下检查：

### JSON 结构

- [ ] 顶层只有规定的核心键，除非程序明确允许扩展
- [ ] `角色档案` 是字符串数组
- [ ] `道具档案` 是字符串数组
- [ ] `场景档案` 是字符串数组
- [ ] `分镜序列` 是对象数组
- [ ] 每个分镜都有 `编号`、`标题`、`时间段`
- [ ] `编号` 是整数且递增
- [ ] `标题` 是字符串
- [ ] `时间段` 是字符串数组

### 剧情可执行性

- [ ] 每个时间段有明确时间范围
- [ ] 时间范围递增且不含明显冲突
- [ ] 每个时间段有主要镜头行为
- [ ] 人物动作有明确主语和对象
- [ ] 关键道具状态清楚
- [ ] 关键对白明确说话者
- [ ] 音效统一使用“音效：”前缀
- [ ] 镜头之间动作和空间连续
- [ ] 时间跳跃被显式说明
- [ ] 每个节点脱离前后镜头后仍具有基本可执行性

### 语言质量

- [ ] 以可视化动作代替抽象概念
- [ ] 少用无法拍摄的心理描述
- [ ] 避免人物代词产生歧义
- [ ] 重要空间关系明确
- [ ] 重要左右手、前后位置、视线方向明确
- [ ] 每个时间段最好有明确的视觉或声音变化

---

## 36. 给其他 AI 的核心指令（一句话版）

> **请把输出视为一个“程序可解析的剧情分镜 JSON DSL”：严格保持既定 JSON 层级、字段名、字段类型和数组结构；使用“整体风格 → 角色档案 → 道具档案 → 场景档案 → 分镜序列 → 时间段”的层级组织；其中 `时间段` 必须保持为按时间递增的自然语言字符串数组，每条字符串按照“时间范围 → 镜头 → 环境/光线 → 人物位置与动作 → 关系/表情 → 对白 → 音效”的逻辑组织；不要擅自改变 JSON 结构，不要增加未经约定的字段，不要把自然语言时间段改造成对象结构，并确保每个分镜节点即使被单独抽取也具备足够上下文可以执行。**

---

## 37. 核心思想总结

这套格式可以概括为：

```text
稳定的 JSON 外壳
    +
可复用的世界/角色/道具/场景档案
    +
按编号排列的剧情节点
    +
按时间切分的自然语言镜头片段
    +
明确的人物关系、动作、对白与声音
    =
可被程序拆分、重组、再交给 AI 执行的剧情分镜 DSL
```

其真正的关键不是“文字写得像小说”，而是：

> **让机器知道怎么拆，让 AI 知道怎么拍，让剧情在拆开之后仍然成立。**
